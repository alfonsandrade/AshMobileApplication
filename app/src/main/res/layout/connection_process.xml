<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mainFragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/gray_bg">

    <ImageView
        android:id="@+id/ashconnectionicon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ash_connection_icon"
        app:layout_constraintBottom_toTopOf="@id/ash_bluetooth_icon"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/ash_bluetooth_icon"
        android:layout_width="178dp"
        android:layout_height="178dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ash_bluetooth_icon"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@android:color/transparent"
        android:paddingRight="20dp"
        app:layout_constraintTop_toBottomOf="@id/ashconnectionicon"/>

    <ImageButton
        android:id="@+id/ash_wifi_icon"
        android:layout_width="178dp"
        android:layout_height="178dp"
        android:src="@drawable/ash_wifi_icon"
        android:background="@android:color/transparent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:scaleType="fitCenter"
        android:paddingLeft="20dp"
        app:layout_constraintTop_toBottomOf="@id/ashconnectionicon"/>


</androidx.constraintlayout.widget.ConstraintLayout>