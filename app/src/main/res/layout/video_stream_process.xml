<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mainFragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/gray_bg">

    <ImageView
        android:id="@+id/battery_icon"
        android:layout_width="100dp"
        android:layout_height="80dp"
        android:padding="5dp"
        android:src="@drawable/ash_battery_0"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:scaleType="fitCenter"
        android:visibility="invisible"/>

    <ImageButton
        android:id="@+id/back_button"
        android:background="@android:color/transparent"
        android:layout_width="100dp"
        android:layout_height="80dp"
        android:padding="5dp"
        android:src="@drawable/back_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:scaleType="fitCenter"/>

    <ImageView
        android:id="@+id/live_frame"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:src="@drawable/ash_live_frame"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/back_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />

    <!-- Player View for Media3 -->
    <androidx.media3.ui.PlayerView
        android:id="@+id/video_view"
        android:layout_width="346dp"
        android:layout_height="445dp"
        android:padding="10dp"
        app:layout_constraintBottom_toBottomOf="@id/live_frame"
        app:layout_constraintEnd_toEndOf="@id/live_frame"
        app:layout_constraintStart_toStartOf="@id/live_frame"
        app:layout_constraintTop_toTopOf="@id/live_frame" />

</androidx.constraintlayout.widget.ConstraintLayout>
